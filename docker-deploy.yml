- hosts: all
  tasks:
    - package:
        name:
          - docker
        state: present
    - name: Create a data container
      docker_container:
        name: docker_img
        image: https://hub.docker.com/_/ghost/
